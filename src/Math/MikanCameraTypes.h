#pragma once

// Shim for MikanXR's MikanCameraTypes.h include. The only content the copied
// code needs from that header is reachable through MikanVideoSourceTypes.h.
#include "MikanVideoSourceTypes.h"
